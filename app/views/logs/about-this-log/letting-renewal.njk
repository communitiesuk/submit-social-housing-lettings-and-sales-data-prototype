{% extends "layouts/question.njk" %}

{% set title = "Is this a renewal to the same tenant in the same property?" %}

{% block question %}
  {{ govukRadios(decorate({
    fieldset: {
      legend: {
        text: macro.heading(title, caption)
      }
    },
    items: data.questions["yes-no"]
  }, ["logs", log.id, section.id, "letting-renewal"])) }}
{% endblock %}
