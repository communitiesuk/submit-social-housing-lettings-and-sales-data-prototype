{% extends "check-answers.njk" %}

{% set isLet = log[section.id]["sale-or-letting"] == "letting" %}
{% set isSale = log[section.id]["sale-or-letting"] == "sale" %}
{% set isCompleted = log[section.id]["completed"] == "true" %}

{% block checkAnswers %}
  {{ govukSummaryList({
    rows: [{
      key: {
        text: "Owning organisation"
      },
      value: {
        text: log[section.id]["organisation-owner"] | textFromInputValue(data.questions["organisation"])
      },
      actions: {
        items: [{
          href: sectionPath + "/organisation?referrer=" + referrer,
          text: "Change",
          visuallyHiddenText: "owning organisation"
        }]
      } if not isCompleted
    }, {
      key: {
        text: "Managing organisation"
      },
      value: {
        text: log[section.id]["organisation-manager"] | textFromInputValue(data.questions["organisation"])
      },
      actions: {
        items: [{
          href: sectionPath + "/organisation?referrer=" + referrer,
          text: "Change",
          visuallyHiddenText: "managing organisation"
        }]
      } if not isCompleted
    }, {
      key: {
        text: "Sale or letting"
      },
      value: {
        text: log[section.id]["sale-or-letting"] | textFromInputValue(data.questions["sale-or-letting"])
      },
      actions: {
        items: [{
          href: sectionPath + "/sale-or-letting?referrer=" + referrer,
          text: "Change",
          visuallyHiddenText: "if sale or letting"
        }]
      } if not isCompleted
    }, {
      key: {
        text: "Property renewal"
      },
      value: {
        text: log[section.id]["letting-renewal"] | textFromInputValue(data.questions["yes-no"])
      },
      actions: {
        items: [{
          href: sectionPath + "/letting-renewal?referrer=" + referrer,
          text: "Change",
          visuallyHiddenText: "if renewal"
        }]
      } if not isCompleted
    } if isLet, {
      key: {
        text: "Tenacy start date"
      },
      value: {
        text: log[section.id]["letting-start-date"] | textFromInputValue
      },
      actions: {
        items: [{
          href: sectionPath + "/letting-start-date?referrer=" + referrer,
          text: "Change",
          visuallyHiddenText: "tenacy start date"
        }]
      } if not isCompleted
    } if isLet, {
      key: {
        text: "Rent type"
      },
      value: {
        text: log[section.id]["letting-rent-type"] | textFromInputValue(data.questions["letting-rent-type"])
      },
      actions: {
        items: [{
          href: sectionPath + "/letting-type?referrer=" + referrer,
          text: "Change",
          visuallyHiddenText: "rent type"
        }]
      } if not isCompleted
    } if isLet, {
      key: {
        text: "Needs type"
      },
      value: {
        text: log[section.id]["letting-need-type"] | textFromInputValue(data.questions["letting-need-type"])
      },
      actions: {
        items: [{
          href: sectionPath + "/letting-type?referrer=" + referrer,
          text: "Change",
          visuallyHiddenText: "needs type"
        }]
      } if not isCompleted
    } if isLet, {
      key: {
        text: "Tenant code"
      },
      value: {
        text: log[section.id]["tenant-code"] | textFromInputValue
      },
      actions: {
        items: [{
          href: sectionPath + "/tenant-code?referrer=" + referrer,
          text: "Change",
          visuallyHiddenText: "tenant code"
        }]
      } if not isCompleted
    } if isLet, {
      key: {
        text: "Sale completion date"
      },
      value: {
        text: log[section.id]["sale-completion-date"] | textFromInputValue
      },
      actions: {
        items: [{
          href: sectionPath + "/sale-completion-date?referrer=" + referrer,
          text: "Change",
          visuallyHiddenText: "sale completion date"
        }]
      } if not isCompleted
    } if isSale, {
      key: {
        text: "Purchaser code"
      },
      value: {
        text: log[section.id]["purchaser-code"] | textFromInputValue
      },
      actions: {
        items: [{
          href: sectionPath + "/purchaser-code?referrer=" + referrer,
          text: "Change",
          visuallyHiddenText: "purchaser code"
        }]
      } if not isCompleted
    } if isSale]
  }) }}
  {% if isCompleted %}
  {{ govukDetails({
    summaryText: "Why can’t I edit this section?",
    html: 'We ask different question based on your responses to this section. If you want to change your answers, you should <a href="#">delete this log</a> and start a new one.'
  }) }}
  {% else %}
  {{ govukWarningText({
    text: "You will not be able to change these answers later on.",
    iconFallbackText: "Warning"
  }) }}
  {% endif %}
{% endblock %}
