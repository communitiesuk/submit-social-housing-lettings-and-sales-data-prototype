{% extends "question.njk" %}

{% set title = "What is the reason for the vacancy?" %}

{% block question %}
  {{ govukRadios(decorate({
    fieldset: {
      legend: {
        text: macro.heading(title, caption)
      }
    },
    items: data.questions["reason-for-vacancy"]
  }, ["logs", log.id, section.id, "reason-for-vacancy"])) }}
{% endblock %}