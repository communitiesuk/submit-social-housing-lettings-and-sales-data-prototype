{% extends "question.njk" %}

{% set title = "Why was the previous tenant evicted?" %}

{% block question %}
  {{ govukRadios(decorate({
    fieldset: {
      legend: {
        text: macro.heading(title, caption)
      }
    },
    items: data.questions["reason-for-vacancy-evicted"]
  }, ["logs", log.id, section.id, "reason-for-eviction"])) }}
{% endblock %}