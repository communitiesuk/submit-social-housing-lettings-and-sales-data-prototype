{% extends "layouts/question.njk" %}

{% set isRelet = log[section.id]["is-relet"] %}
{% if isRelet == "true" %}
  {% set title = "How many times has the property been previously offered since becoming available for re-let?" %}
{% else %}
  {% set title = "How many times has the property been previously offered since becoming available?" %}
{% endif %}
{% set questionId = "times-previously-offered" %}

{% block question %}
  {{ govukInput(decorate({
    classes: "govuk-input--width-2",
    label: {
      html: macro.heading(title, caption)
    },
    hint: {
      text: "If the property is being offered for let for the first time, enter ‘0’."
    },
    inputmode: "numeric",
    pattern: "[0-9]*"
  }, ["logs", log.id, section.id, questionId])) }}
{% endblock %}
