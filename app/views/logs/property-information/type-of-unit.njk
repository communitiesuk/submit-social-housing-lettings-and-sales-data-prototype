{% extends "layouts/question.njk" %}

{% set title = "What type of unit is the property?" %}
{% set questionId = "type-of-unit" %}
{% set needsType = log["setup"]["type-of-need"] %}

{% block question %}
  {{ govukRadios({
    decorate: ["logs", log.id, section.id, questionId],
    fieldset: {
      legend: {
        text: macro.heading(title, caption)
      }
    },
    items: data.questions[questionId][needsType]
  }) }}
{% endblock %}
