{% extends "layouts/question.njk" %}

{% set title = "What client groups is this service for?" %}
{% if scheme["client-groups"] %}
  {# Editing an existing scheme #}
  {% set caption = scheme.name %}
  {% set formAction = schemePath %}
  {% set hasAnsweredQuestion = true %}
  {% set buttonText = "Continue" %}
{% endif %}

{% block question %}
  {{ govukCheckboxes({
    decorate: ["schemes", scheme.id, "client-groups"],
    fieldset: {
      legend: {
        html: macro.heading(title, caption)
      }
    },
    hint: {
      text: "Select up to 2 client groups."
    },
    items: data.questions["client-groups"]
  }) }}
{% endblock %}
