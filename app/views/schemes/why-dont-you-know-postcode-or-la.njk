{% extends "layouts/question.njk" %}

{% set title = "Give a reason why you don’t know the postcode or local authority" %}
{% if scheme["why-dont-you-know-postcode-or-la"] %}
  {# Editing an existing scheme #}
  {% set caption = scheme.name %}
  {% set buttonText = "Save changes" %}
  {% set formAction = schemePath %}
{% endif %}

{% block question %}
  {{ govukTextarea(decorate({
    label: {
      text: macro.heading(title, caption)
    }
  }, ["schemes", scheme.id, "why-dont-you-know-postcode-or-la"])) }}
{% endblock %}
